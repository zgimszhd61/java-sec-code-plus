# 所有文本文件使用 LF 行结束符
* text eol=lf

# Windows 命令脚本应使用 CRLF 行结束符
*.cmd eol=crlf

# 明确声明需要归一化的文本文件
*.java text
*.xml text
*.properties text
*.yml text
*.json text

# 标记二进制文件，防止 Git 进行文本处理
*.png binary
*.jpg binary
*.jar binary

# 对于 shell 脚本，使用 LF 行结束符
*.sh text eol=lf

# Python 文件应使用 LF 行结束符，以便可以通过 shebang 运行
*.py text eol=lf
